# Combine expressions from all grammar definitions
loop = acc_loop | doLoop | whileLoop

statement = accKernelBodyDirective | comment | printStatement | controlStatements | subroutineCall | ifElseBlock | singleLineIf | selectCaseBlock | loop | matrixAssignment | complexAssignment | assignment | funcCall

block <<= Group(ZeroOrMore(statement))
