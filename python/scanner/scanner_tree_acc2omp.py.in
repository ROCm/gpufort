class Acc2Omp():
    def hipFStr(self):
        print("".join(self._lines))
        return "! here would be an OpenMP directive instead", True

class Acc2OmpLoopKernel():
    def hipFStr(self):
        print(self._lines)
        fSnippet = "".join(self._lines)
        parseResult = translator.loopKernel.parseString(fSnippet)
        return parseResult.ompFStr(fSnippet), True 
