CHARACTER
CHARACTER,INTENT(IN)
CHARACTER(LEN=1)
CHARACTER(LEN=1),INTENT(IN)
character(len=20)
character(len=256)
CHARACTER(LEN=38),SAVE
CHARACTER(LEN=40),SAVE
CHARACTER(LEN=6)
CHARACTER(LEN=80)
character(len=*),intent(in)
CHARACTER(LEN=*),INTENT(IN)
CHARACTER(LEN=MPI_MAX_PROCESSOR_NAME),allocatable
complex(DP)
COMPLEX(DP)
complex(dp),allocatable
complex(DP),allocatable
COMPLEX(DP),allocatable
COMPLEX(DP),ALLOCATABLE
complex(DP),allocatable,device
COMPLEX(DP),ALLOCATABLE,DEVICE
COMPLEX(DP),ALLOCATABLE,DIMENSION(:,:)
complex(dp),allocatable,intent(inout)
complex(DP),device
complex(DP),DEVICE
COMPLEX(DP),DEVICE,INTENT(INOUT)
COMPLEX(DP),DEVICE,INTENT(OUT)
COMPLEX(DP),INTENT(IN)
COMPLEX(DP),INTENT(INOUT)
COMPLEX(DP),INTENT(INOUT),DEVICE
complex(dp),intent(out)
COMPLEX(DP),INTENT(OUT)
COMPLEX(DP),INTENT(OUT),DEVICE
COMPLEX(DP),PARAMETER
COMPLEX(DP),VARTYPE
complex(real32),dimension(:),intent(in)
complex(real32),intent(in)
complex(real32),intent(in),dimension(:)
complex(real64),dimension(:),intent(in)
complex(real64),intent(in)
complex(real64),intent(in),dimension(:)
integer
INTEGER
integer,allocatable
integer,ALLOCATABLE
INTEGER,allocatable
INTEGER,ALLOCATABLE
INTEGER,DEVICE
integer(int16),dimension(:),intent(in)
integer(int16),intent(in)
integer(int32)
integer(int32),dimension(:),intent(in)
integer(int32),intent(in)
integer(int64),dimension(:),intent(in)
integer(int64),intent(in)
integer(int8),dimension(:),intent(in)
integer(int8),intent(in)
integer,intent(in)
integer,INTENT(IN)
INTEGER,intent(in)
INTEGER,INTENT(IN)
INTEGER,INTENT(INOUT)
integer,intent(out)
INTEGER,INTENT(OUT)
INTEGER,INTENT(OUT),ALLOCATABLE
INTEGER,OPTIONAL,INTENT(OUT)
integer,parameter
INTEGER,PARAMETER
INTEGER,VARTYPE
logical
LOGICAL
logical,intent(in)
LOGICAL,INTENT(IN)
logical,intent(in),dimension(:)
logical,intent(in),optional
LOGICAL,INTENT(OUT)
LOGICAL,OPTIONAL
LOGICAL,OPTIONAL,INTENT(OUT)
LOGICAL,SAVE
REAL*8
REAL*8,allocatable
real(dp)
real(DP)
REAL(DP)
real(dp),allocatable
real(DP),allocatable
REAL(DP),allocatable
REAL(DP),ALLOCATABLE
real(DP),allocatable,device
REAL(DP),ALLOCATABLE,DEVICE
REAL(DP),ALLOCATABLE,DIMENSION(:,:)
real(dp),allocatable,intent(inout)
real(DP),device
real(DP),DEVICE
REAL(DP),DEVICE
real(DP),DEVICE,allocatable
REAL(DP),DEVICE,INTENT(IN)
REAL(DP),DEVICE,INTENT(INOUT)
REAL(DP),DEVICE,INTENT(OUT)
real(DP),intent(in)
REAL(DP),INTENT(IN)
REAL(DP),INTENT(IN),DEVICE
REAL(DP),INTENT(INOUT)
REAL(DP),INTENT(INOUT),DEVICE
REAL(DP),INTENT(OUT)
REAL(DP),INTENT(OUT),DEVICE
REAL(DP),PARAMETER
REAL(DP),VARTYPE
real(real32)
real(real32),dimension(:),intent(in)
real(real32),intent(in)
real(real32),intent(in),dimension(:)
real(real32),intent(in),optional
real(real64)
real(real64),dimension(:),intent(in)
real(real64),intent(in)
real(real64),intent(in),dimension(:)
real(real64),intent(in),optional
REAL(SP)
real(SP),allocatable
REAL(SP),ALLOCATABLE
REAL(SP),INTENT(IN)
type
TYPE(cusolverDnHandle)
TYPE(la_descriptor)
TYPE(la_descriptor),INTENT(IN)
TYPE(la_descriptor),INTENT(OUT)
TYPE(tester_t)
